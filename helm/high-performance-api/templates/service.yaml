apiVersion: v1
kind: Service
metadata:
  name: {{ include "high-performance-api.fullname" . }}
  labels:
    {{- include "high-performance-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "high-performance-api.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
